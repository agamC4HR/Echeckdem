using Echeckdem.Models;

namespace Echeckdem.Services
{
    public interface ITrackScopeSetupService
    {
        void AddTrackScope(TrackScope trackScope);
        //List<TrackScope> GetAllTrackScopes();
        //TrackScope GetTrackScopeById(int id);
    }
}
